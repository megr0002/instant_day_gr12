---
import MainLayout from "../layouts/MainLayout.astro";
import Link from "../components/Link.astro";
import Heading from "../components/Heading.astro";
import Text from "../components/Text.astro";
import Button from "../components/Button.astro";
---

<footer class="w-full border-t-[2px] border-secondary-700 bg-primary-400">
  {/* Heading fylder hele bredden */}
  <div class="w-full pt-8 md:py-8 md:break-all">
    <Heading
      size="h1"
      htmlheading="h1"
      stylingclasses="text-tertiary-400 text-[95px] lg:text-[218px] text-center lg:text-left md:word-break: break-all overflow-hidden"
    >
      INSTANT DAY
    </Heading>
  </div>

  {/* Tekst + knapper i flex container */}
  <div
    class="w-full py-8 md:py-3 flex flex-col md:flex-row md:justify-between gap-8 px-5 md:px-40 md:pb-20"
  >
    {/* Venstre side - Tekst */}
    <Text stylingclasses="leading-relaxed  max-w-xl">
      Instant Day is an international
      <span class="text-tertiary-400"> celebration</span> of instant photography.
      Every year on
      <span class="text-tertiary-400"> October 18</span>, photographers around
      the world share their instants — not as a competition, but as a tribute to
      the <span class="text-tertiary-400">analogue</span>, the
      <span class="text-tertiary-400">tactile</span>, and the
      <span class="text-tertiary-400">joy</span> of capturing life in the moment.
    </Text>

    {/* Højre side - Knapper */}
    <div class="flex flex-col items-end gap-8 justify-end">
      <Button stylingclasses="mr-5 md:w-73 ">SUBMIT YOUR INSTANT</Button>
      <Button stylingclasses="mr-5 md:w-73 ">CONTACT US BY E-MAIL</Button>
    </div>
  </div>
</footer>
